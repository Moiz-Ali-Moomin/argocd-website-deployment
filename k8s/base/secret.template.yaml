apiVersion: v1
kind: Secret
metadata:
  name: argocd-website-secret
type: Opaque
stringData:
  # ------------------------------------------------------------------
  # WARNING: DO NOT COMMIT ACTUAL SECRETS TO GIT
  # Use this template to create the secret manually in the cluster.
  # ------------------------------------------------------------------
  API_KEY: "your-api-key-here"
