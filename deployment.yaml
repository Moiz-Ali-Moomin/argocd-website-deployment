apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-website
  labels:
    app: argocd-website
spec:
  replicas: 5
  selector:
    matchLabels:
      app: argocd-website
  template:
    metadata:
      labels:
        app: argocd-website
    spec:
      containers:
        - name: nginx
          image: ghcr.io/moiz-ali-moomin/argocd-website:f4be1dbb0f7704d432537396014a8d7393b16b37
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
